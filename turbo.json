{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "outputs": ["dist/**"],
      "env": ["TASKLESS_BUILD_TARGET"]
    },
    "test": {
      "dependsOn": ["build"]
    },
    "typecheck": {}
  }
}

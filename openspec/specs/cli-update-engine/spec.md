# CLI Update Engine

## Purpose

This capability has been removed. The `update-engine` subcommand and all associated backend endpoints have been decommissioned. The CLI no longer manages scaffold upgrades.

## Requirements

_All requirements have been removed. This spec is retained for historical reference._
